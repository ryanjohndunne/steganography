#ifndef PARALLEL_H_FILE
#include <stdio.h>
#define PARALLEL_H_FILE

// Parallel program
void parallelProgram(char **argv);
// Checks that the format of the file is correct for parallel execution
void correctFormatFile(FILE *);

#endif
